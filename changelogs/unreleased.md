# Unreleased

- Docs: improve website discoverability and consistency for `app.throw` details, runtime hooks, response cache, and Apache-2.0 license wording.
- Breaking: raise the framework Node.js baseline to `>=20.19.0`, move `@koa/router` out of production dependencies as an optional peer, and replace the `mongodb-memory-server` wrapper with `mongodb-memory-server-core` for controlled test binary downloads.
