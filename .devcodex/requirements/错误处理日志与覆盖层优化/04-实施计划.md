# 实施计划 — 错误处理日志与覆盖层优化

> **项目**: vext
> **创建日期**: 2026-04-13
> **状态**: CP3 已确认，待执行
> **关联需求**: [01-需求概述.md](./01-需求概述.md)
> **关联方案**: [02-技术方案.md](./02-技术方案.md)

## 任务列表

| # | 文件 | 类型 | 状态 |
|---|------|------|------|
| T1 | `src/types/app.ts` | 修改 | ✅ 完成 |
| T2 | `src/lib/middlewares/error-handler.ts` | 修改 | ✅ 完成 |
| T3 | `src/lib/bootstrap.ts` | 修改 | ✅ 完成 |
| T4 | `src/lib/dev/dev-bootstrap.ts` | 修改 | ✅ 完成 |
| T5 | `test/unit/lib/error-handler.test.ts` | 新建 | ✅ 完成 |
| T6 | 运行测试验证 | 验证 | ✅ 11/11 新增 + 13/13 回归，全通过 |

## T1 — 类型扩展

**文件**：`src/types/app.ts`

- 在 `VextResponseConfig` 接口前新增 `VextLogErrorsConfig` 接口
- `VextResponseConfig` 追加 `logErrors?: VextLogErrorsConfig` 字段

## T2 — error-handler 实现

**文件**：`src/lib/middlewares/error-handler.ts`

- import 第 2 行追加 `VextLogger`
- `createErrorHandler` 增加第 3 个可选参数 `logger?: VextLogger`
- 闭包内：**日志记录逻辑前置**（位于 devOverlay 判断之前）
  - 类型判断分支：VextValidationError → 不记录；HttpError → 按 status 分级；其他 → error
  - 每个分支检查对应的 `logErrors.*` 开关（默认值规则见 §3.2）

## T3 — 生产 Bootstrap

**文件**：`src/lib/bootstrap.ts` 第 367 行

- `createErrorHandler(config.response ?? {})` → `createErrorHandler(config.response ?? {}, undefined, app.logger)`

## T4 — Dev Bootstrap

**文件**：`src/lib/dev/dev-bootstrap.ts`

- 第 468 行：`createErrorHandler(config.response ?? {}, overlayFn)` → 追加 `app.logger`
- 第 619 行（SoftReloader 闭包）：`createErrorHandler(...)` → 追加 `app.logger`

## T5 — 单元测试

**文件**：`test/unit/lib/error-handler.test.ts`（新建）

共 9 个测试用例（见 §5.1 测试分组）：
- 向后兼容：不传 logger 不报错
- 未知错误：error 被调用 / unknownErrors:false 不调用 / VextValidationError 不调用
- HttpError 5xx：error 被调用 / http5xx:false 不调用
- HttpError 4xx：默认不调用 / http4xx:true 调用
- devOverlay 路径：logger.error 仍被调用（日志先于响应格式）

## T6 — 测试验证

```bash
# 新增测试
npx vitest run test/unit/lib/error-handler.test.ts

# 回归测试（确认 overlay 测试不受影响）
npx vitest run test/unit/lib/error-handler-overlay.test.ts
```

期望：全部 PASS，无新增 error

