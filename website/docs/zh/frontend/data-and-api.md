# 数据与 API 调用

本页仅为旧链接保留。

请使用新的拆分页面：

- [数据流](/zh/frontend/data-flow)：route services、render props、layoutData、messages 和 render cache 边界。
- [API Client 与契约](/zh/frontend/api-client-and-contracts)：生成 client artifacts 与外部前端适配契约。
- [Render Data 与缓存](/zh/frontend/render-data-and-cache)：`res.render()` 复用 route cache 的规则。

新文档应链接到这些页面，而不是这个兼容入口。
