# Data and API Calls

This page is kept as a compatibility entry for older links.

Use the new focused pages:

- [Data Flow](/frontend/data-flow) for route services, render props, layoutData, messages, and render cache boundaries.
- [API Client and Contracts](/frontend/api-client-and-contracts) for generated client artifacts and external frontend adapter contracts.
- [Render Data and Cache](/frontend/render-data-and-cache) for route cache reuse with `res.render()`.

New documentation should link to those pages instead of this compatibility entry.
